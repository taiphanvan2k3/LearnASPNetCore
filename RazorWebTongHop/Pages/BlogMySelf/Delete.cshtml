@page
@model RazorWebTongHop.Pages.BlogMySelf.DeleteModel
@{
}

<h1>Xóa</h1>
<h2>Bạn có chắc xóa bài viết?</h2>

<dl class="row">
    <dt class="col-md-2">@Html.DisplayNameFor(model => model.Article.Title)</dt>
    <dd class="col-md-10">@Html.DisplayFor(model => model.Article.Title)</dd>
    <dt class="col-md-2">@Html.DisplayNameFor(model => model.Article.CreateAt)</dt>
    <dd class="col-md-10">@Html.DisplayFor(model => model.Article.CreateAt)</dd>
</dl>

<form method="POST">
    <input asp-for="Article.Id" type="hidden">
    <button class="btn btn-danger">Xóa</button>
    <a class="btn btn-success" asp-page="./Index">Về trang danh sách</a>
</form>